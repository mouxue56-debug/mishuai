[project]
name = "fukuraku-ai-secretary"
version = "0.1.0"
description = "AI Secretary for Fukuraku Cattery"
requires-python = ">=3.10"
readme = "README.md"

[project.scripts]
fukuraku = "src.main:main"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
